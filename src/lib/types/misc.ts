export interface Location {
    country: {
        name: string;
        code: string;
    };
    currencyCode: string;
    locales: string[];
}

export interface Color {
  name: string;
  value: string;
}
